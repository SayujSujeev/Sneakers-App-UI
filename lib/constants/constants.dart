import 'package:flutter/material.dart';

//Padding
const double appPadding = 20.0;

//Main Colors used in this app
const primaryColor = Colors.white;
const secondaryColor = Color(0xFF1A237E);
const textColor = Color(0xFFEC407A);